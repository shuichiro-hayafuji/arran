#!/usr/bin/env bash
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
if ! command -v python3 >/dev/null 2>&1; then
  echo '[BLOCKED] Python 3 is required. Prepare it manually and retry; see tools/HOOKS.md.' >&2
  exit 1
fi
exec python3 -B tools/git_hooks.py pre-commit
